<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- بلا هذا السطر لا يصل التطبيق إلى الشبكة في نسخة الإصدار إطلاقاً.
         قالب Flutter يضع الإذن في بياني التنقيح والتشخيص وحدهما، فيعمل عندك
         على السلك ويفشل كل نداء لـSupabase على جهاز المستخدم برسالةٍ غامضة.
         والتطبيق كلّه قراءةٌ من الشبكة، فالإذن شرطُ عمله لا تحسينٌ فيه. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- أندرويد ١٣ فما فوق لا يعرض إشعاراً حتى يُؤذَن صراحةً، والإذن يُطلب
         وقت التشغيل. وبلا هذا السطر لا يظهر الطلب أصلاً ولا يصل إشعارٌ واحد
         على أجهزة السنوات الثلاث الأخيرة — بلا خطأ في أي سجلّ. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- الرسالة الصوتية في المحادثة. والإذن يُطلب وقت التشغيل كذلك، فالسطر
         هنا شرطٌ لظهور الطلب لا بديلٌ عنه: بدونه يعود `hasPermission` بـ
         `false` أبداً، فيضغط المستخدم زرّ التسجيل ولا يقع شيء ولا تظهر له
         رسالةٌ تقول لماذا. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <!-- زرُّ «موقعي الحالي» في منتقي الخريطة، ولا شيءَ غيره: التطبيق لا يتتبّع
         أحداً ولا يقرأ موقعاً في الخلفيّة. والإذنُ يُطلب وقت التشغيل بضغطة
         صاحبه وهو واقفٌ في المكان يضع دبّوسه.

         و`COARSE` إلى جانب `FINE` مقصودة: من رفض الدقيق يعطي التقريبيَّ أحياناً
         (أندرويد ١٢ فما فوق يعرض الخيارين)، ودقّةُ حيٍّ تكفي لتحريك الخريطة
         إلى بلده — ثمّ يضبط الدبّوس بإصبعه. ورفضُ كلِّ شيءٍ يُقال له نصّاً
         ولا يترك زرّاً ميّتاً. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- قناة الإشعارات الافتراضية وأيقونتها.
             بلا القناة يضع أندرويد ٨ فما فوق الإشعارات في قناةٍ مجهولة لا
             يستطيع المستخدم ضبطها. وبلا الأيقونة يرسم النظام مربّعاً رمادياً
             مكان الرمز — وهي أيقونةٌ صامتة أحادية اللون، لا أيقونة التطبيق. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
