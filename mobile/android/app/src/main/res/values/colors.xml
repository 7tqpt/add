<?xml version="1.0" encoding="utf-8"?>
<!-- ألوانُ نافذة أندرويد — تُرسم قبل أن يقلع محرّك Flutter، فما يظهر في
     تلك اللحظة من أندرويد لا من دارت. ولذلك تُكتب هنا مرّةً ثانية.

     **وقيمتُها تُقاس بقيمة `AppColors` في `theme.dart`** في
     `test/splash_window_test.dart`: لونان مكتوبان بيدٍ في ملفّين يفترقان
     مع أوّل تعديلٍ للهويّة، ولا يظهر افتراقُهما إلّا ومضةً في الإقلاع. -->
<resources>
    <!-- كريمُ الصفحة — `AppColors.page`. باقٍ لأنّ `ic_launcher_background`
         وغيرَه قد يحتاجه، ولأنّ حذفَ لونٍ مسمّى تبديلٌ لا داعيَ له. -->
    <color name="brand_page">#FBF4EF</color>

    <!-- نبيذيُّ الإقلاع — `AppColors.accentDeep`، وهو أغمقُ طرفَي تدرّج
         `BrandBackdrop` الذي ترسمه `BootScreen`. فالنافذةُ تصل إلى الشاشة
         بلا ومضة. -->
    <color name="brand_splash">#5C0820</color>
</resources>
