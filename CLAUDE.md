# منصّة «فرحتي» — قواعدُ العمل

## ١) صورةٌ قبل التنفيذ، وسؤالٌ بعدها — **قاعدةٌ لا استثناء**

قرّر صاحبُ المنصّة: **لا يُلمَس شيءٌ في `lib/` أو `src/` قبل أن تُعرض عليه
صورةُ ما سيصير، وقبل أن يُسأل ويجيب.**

وأصلُها أنّي نفّذتُ حقولَ «تقديم خدمة» ودفعتُها قبل أن أُريَه، فنبّهني:
«عند طلب تقديم خدمه جيب صورة قبل تنفيذ». ثمّ عمّمها: «من الان ايش تسويه
تجيب لي صورة وتسالني».

### كيف تُصنع الصورة

راسمٌ في `mobile/tool/` — **خارجَ `test/` فلا يُدرج في الحزمة** — يُسمّى
`*_proposal_test.dart` للمقترح، و`*_shot_test.dart` لتصوير ما صار:

```bash
export PATH=/opt/flutter/bin:$PATH
cd mobile
SHOTS=<مجلّد> flutter test tool/<الراسم>.dart
```

ثمّ تُرسَل الصورةُ إليه بـ`SendUserFile`.

وأمثلتُها في الشجرة: `one_category_proposal_test.dart` (مقترحٌ قبل التنفيذ)،
و`dropdowns_shot_test.dart` (تصويرُ الشاشة الحقيقيّة بعده).

### وما يُرسم يجب أن يكون صادقاً

- **الشاشةُ الحقيقيّةُ لا رسمٌ يشبهها** حيث أمكن: يُبنى العنصرُ نفسُه
  بثيمة التطبيق (`buildTheme()`)، لا صندوقٌ مرسومٌ بالألوان.
- **والخطوطُ تُحمَّل** بـ`FontLoader`، وإلّا خرجت الحروفُ مربّعاتٍ بيضاء.
- **وحدُّ الرسم (`RepaintBoundary`) خارجَ `MaterialApp`** إن كان المصوَّرُ
  قائمةً منسدلةً أو ورقةً سفليّة: تلك تُدفع طرقاً في `Overlay` الملاحة —
  خارجَ أيّ حدٍّ في `home`. وقد خرجت ثلاثُ لقطاتٍ متطابقةٍ إلى البايت قبل
  أن أفهم ذلك. (وراسمُ اللقطة يتعلّق على منسدلةٍ مفتوحةٍ حتى مع ذلك، فتلك
  تُقاس بشجرة العناصر لا بالصورة.)
- **وإن رُسم ما لا يمكن تصويرُه فليُقَل إنّه مرسوم.**

### ثمّ يُسأل ويُنتظر

يُعرض عليه الخياراتُ مسمّاةً (أ) و(ب) و(ج)، ولا يُمضى حتى يجيب. وجوابُه قد
يكون كلمةً واحدة — «(ب) العمود الرأسيّ» أو «القسم» — وهي إذنٌ كافٍ.

## ٢) القياسُ قبل الدفع

- `flutter analyze` نظيفٌ، و`flutter test` خضراءُ كلُّها.
- **وضوابطُ سالبةٌ لكلّ ضمانةٍ جديدة:** سكربتٌ في `mobile/tool/controls_*.sh`
  يكسر الشيفرةَ كسراً واحداً ويتأكّد أنّ الحزمةَ تحمرّ. وما لا يسقط بالكسر
  ضمانةٌ كاذبة. (وقد كشفت هذه الضوابطُ عيوباً حقيقيّةً مرّاراً: `sound`
  مكتوبةٌ مرّتين، ولونَ خيطٍ لم يقسه شيء، ومنسدلةً تُسأل عمّا تعرضه لا عمّا
  يصل الخادم.)
- **ولا يُسأل الحقلُ عمّا فيه:** `DropdownButtonFormField` يحتفظ باختياره
  داخلَ نفسه فيعرضه للعين ولو لم يصل الخادمَ شيء. فيُقاس **ما وصل**.
- والنسخةُ تُرفع في موضعين معاً: `mobile/pubspec.yaml` و
  `mobile/lib/src/core/app_version.dart`.

## ٣) الأسرار

- المفتاحُ العامّ (`sb_publishable_…` / anon) مصمَّمٌ ليعمل في المتصفّح
  والتطبيق — الحرزُ هو RLS — فموضعُه `.env` وأسرارُ المضيف و`--dart-define`.
- **وكلمةُ القاعدة ومفتاحُ `service_role` لا يُطلبان ولا يُكتبان في ملفٍّ
  يُشحَن**: يتجاوزان RLS كلَّها.
- **ومفتاحُ خدمةِ فايربيز سرٌّ حقيقيّ**: موضعُه أسرارُ دوالّ Supabase
  (`FCM_SERVICE_ACCOUNT`) وحدَها — لا المستودعُ ولا `env.json`، **ولا يُرسل
  في المحادثة: ما أُرسل فيها احترق ووجب تبديلُه**.
- ولا يصل العميلُ ولا مقدّمُ الخدمة إلى لوحة التحكّم.

## ٤) الفروع

التطويرُ على `claude/mobile-app-dashboard-gsbgq6`، و«**دمج**» تعني: طلبُ دمجٍ
إلى `main`، ثمّ إطلاقُ `apk.yml` على `main` فيصير الوسمُ `build-<رقم الجولة>`.
وبعد الدمج بالتجزئة يُعاد الفرعُ من رأس `main`:

```bash
git checkout main -q && git pull -q origin main
git checkout -B claude/mobile-app-dashboard-gsbgq6 -q
git push --force-with-lease -u origin claude/mobile-app-dashboard-gsbgq6
```

## ٥) البيئة

`flutter` ليس في المسار — `/opt/flutter/bin`. فكلُّ أمرٍ يبدأ بـ
`export PATH=/opt/flutter/bin:$PATH`.
