node_modules
dist
dist-ssr
*.local
.env
.env.local
.env.*.local
.DS_Store
*.log
npm-debug.log*
.vite
tsconfig.tsbuildinfo
*.tsbuildinfo

# ذاكرة بايثون المؤقّتة — تولّدها سكربتات مهارات `.claude/skills` عند تشغيلها،
# وهي ناتجُ تصريفٍ يخصّ جهازاً ونسخةَ مفسّرٍ بعينهما، فلا تُودَع في مستودع.
__pycache__/
*.py[cod]

# ناتج بناء تطبيق سطح المكتب — ثنائيّات ومُثبِّتات تُبنى من المصدر، وحجمها
# مئات الميغابايت. و`gen/schemas` يولّدها Tauri من الإعدادات عند كل بناء.
src-tauri/target/
src-tauri/gen/
