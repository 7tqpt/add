{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "ما تملكه نافذة اللوحة: تُظهر نفسها بعد التحميل، وتحفظ تقريراً، وتفتح رابطاً خارجياً في المتصفّح. لا أكثر.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-show",
    "core:window:allow-set-focus",
    "dialog:allow-save",
    "opener:allow-open-url"
  ]
}
